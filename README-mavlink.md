To test: build SITL, set MC_NN_OFFB=1, MC_NN_MANL_CTRL=0, MAV_FWDEXTSP=1, reboot, then run the test script. You should now see listener trajectory_setpoint updating at ~10 Hz while  
  the script runs.
